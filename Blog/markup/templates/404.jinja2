{% extends "base.jinja2" %}

{% block title %}{{_('Нет такой страницы')}}{% endblock title %}

{% block content %}
    <p>{{_('К сожалению, по этому запросу ничего не найдено. Попробуйте вернуться на главную и поискать нужную информацию в
            других разделах.')}}</p>
    <a href="{{ url('home') }}">
        <button class="btn btn-primary text-black">
            {{_('На главную')}}
        </button>
    </a>
{% endblock content %}